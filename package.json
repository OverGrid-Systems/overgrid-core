{
  "name": "overgrid-core",
  "private": true,
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node scripts/dev_admin_server.cjs",
    "ci:all": "npm run -s ci:all || true"
  }
}
